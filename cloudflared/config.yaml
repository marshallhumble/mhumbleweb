tunnel: my-personal-site-tunnel
credentials-token: ${TUNNEL_TOKEN}

ingress:
  - hostname: mhumble.io
    service: https://localhost:443
    originRequest:
      noTLSVerify: false
      http2Origin: true

  - service: http_status:404
